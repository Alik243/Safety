<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/bootstrap.css">
    <title>Mergen | <%= title %></title>

    <style>
        body {
            position: absolute;
            width: 100%;
            height: 100%;
            background-color: #ececec;

            display: flex;
            justify-content: center;
            align-items: center;
        }

        .card_block {
            background-color: #fff;
            width: 300px;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 10px;
            text-align: center;
            box-shadow: 0 0 16px rgba(0, 0, 0, 0.15);
        }

        .card_header {
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 16px;
        }
    </style>
</head>
<body>
    <div class="card_block">
        <div class="card_header">
            Авторизация
        </div>
    
        <div style="text-align: start;">
            <input type="text" id="auth-login" class="form-control" placeholder="Введите логин">

            <input type="password" id="auth-password" class="form-control my-2" placeholder="Введите пароль">

            <button class="btn btn-primary mt-2 w-100">Применить</button>
        </div>
    </div>
</body>
</html>